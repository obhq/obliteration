name: PR Build
on:
  pull_request:
    branches:
    - '*'
concurrency: pr-${{ github.ref }}
jobs:
  quality-checks:
    name: QC
    uses: ./.github/workflows/qualitychecks.yml
  windows:
    needs: QC
    name: Build
    uses: ./.github/workflows/ci-windows.yml
  linux:
    needs: QC
    name: Build
    uses: ./.github/workflows/ci-linux.yml
  mac:
    needs: QC
    name: Build
    uses: ./.github/workflows/ci-mac.yml
