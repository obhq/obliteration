import { StandardButton, VerticalBox } from "std-widgets.slint";

export component NewProfile inherits Dialog {
    title: "New Profile";

    VerticalBox { }

    StandardButton {
        kind: ok;
    }

    StandardButton {
        kind: cancel;
    }
}
