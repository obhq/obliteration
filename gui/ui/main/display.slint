import { ComboBox, HorizontalBox, VerticalBox, GroupBox } from "std-widgets.slint";

export component DisplayTab {
    in property <[string]> devices;
    in-out property <int> selected-device;
    in property <[string]> resolutions;
    in-out property <int> selected-resolution;

    VerticalBox {
        padding-top: 0;
        alignment: start;

        HorizontalBox {
            padding: 0;

            GroupBox {
                title: "Device";
                width: 50%;
                ComboBox {
                    model: root.devices;
                    current-index <=> root.selected-device;
                }
            }

            GroupBox {
                title: "Resolution";
                ComboBox {
                    model: root.resolutions;
                    current-index <=> root.selected-resolution;
                }
            }
        }
    }
}
