[workspace]
members = [
    "core",
    "elf",
    "error",
    "fs",
    "ftp",
    "gmtx",
    "kernel",
    "kernel-macros",
    "llt",
    "param",
    "pfs",
    "pkg",
    "system",
    "tls"
]

default-members = ["core", "kernel"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
