[workspace]
members = [
    "core",
    "elf",
    "error",
    "fs",
    "ftp",
    "kernel",
    "kernel-macros",
    "param",
    "pfs",
    "pkg",
    "system"
]

default-members = ["core", "kernel"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
