[workspace]
members = [
    "core",
    "elf",
    "error",
    "fs",
    "ftp",
    "kernel",
    "kernel-macros",
    "pfs",
    "pkg",
    "system",
    "util"
]

default-members = ["core", "kernel"]
