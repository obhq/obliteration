[package]
name = "core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
error = { path = "../error" }
param = { path = "../param" }
pkg = { path = "../pkg" }
system = { path = "../system" }
